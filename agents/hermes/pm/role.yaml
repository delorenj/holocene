# Hermes agent role manifest.  Tracked in the PROJECT repository.
# The agent's actual state (memory, sessions, evolving SOUL) lives in the
# runtime submodule.  This file is the immutable contract for this role.

repo: holocene
role: pm
agent_id: holocene-pm
display_name: "Holocene PM"
purpose: "pm agent for holocene"

# Inference target (empty = inherit from global ~/.hermes/config.yaml)
model:
  provider: ""
  name: ""

# Hermes profile name. Profile dir is symlinked into ./runtime/.
profile: holocene-pm

# Telegram bot identity (one bot per agent — see docs/architecture.md).
telegram:
  bot_username: "HolocenePMBot"
# Ticket-provider projection binding (linear | plane | trello), supplied as
# PJangler/bootstrap identity. The current sentinel adapter can write directly,
# but that is a legacy path. Target state changes use the versioned/idempotent
# Lifecycle command contract; this binding never becomes lifecycle authority.
ticket_provider:
  name: plane
  board_id: "727a2b17-a1dd-46f6-b583-afa5a2d2cdae"
  board_url: "https://plane.delo.sh/33god/projects/727a2b17-a1dd-46f6-b583-afa5a2d2cdae/issues/"
  workspace: "33god"
  project: "727a2b17-a1dd-46f6-b583-afa5a2d2cdae"
  # Optional normalized-state name overrides for non-standard boards.
  in_review: ""   # e.g. "In Review"
  completed: ""   # e.g. "Done"

# Scrum Master sentinel knobs (only meaningful for role: scrum-master).
scrum_master:
  grace_hours: 24            # delegated-review grace window (hours)
  auto_review: true          # autonomous delegated-review escape hatch on/off
  paired_pm: "holocene-pm"

# Plane project (1:1 with this agent) — retained for fleet-registry back-compat.
plane:
  # Empty -> resolved from ~/.config/hermes-agent-template/config.toml [plane].workspace
  workspace: ""
  # identifier is set by .scripts/42-ticket-provider.sh after creation (plane only)
  identifier: "HOLOC"
# Bloodbank wiring (consumed + produced subjects).
bloodbank:
  subscribe:
    - "bloodbank.evt.v1.repo.holocene.>"
    - "bloodbank.cmd.v1.agent.holocene-pm.>"
  producer: "hermes-agent:holocene-pm"

# Runtime repo (git-tracked HERMES_HOME, auto-checkpointed).
runtime:
  # Empty owner -> resolved from config.toml [github].runtime_repo_owner.
  github_owner: ""
  github_repo: "agent-hm-holocene-pm"
  submodule_path: "./runtime"
  checkpoint:
    cadence: "hourly"
    on_session_end: true

# Provenance — managed by the template, do not hand-edit.
provisioned_at: "2026-06-01T14:38:43Z"
provisioned_by: hermes-agent-template
