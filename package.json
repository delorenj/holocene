{
  "name": "holocene",
  "private": true,
  "version": "0.0.1",
  "description": "Holocene: 33GOD dashboard and renderer",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "prove:lifecycle-browser": "node scripts/prove-lifecycle-browser.mjs",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "playwright": "1.54.0",
    "turbo": "^2.0.14",
    "typescript": "^5.6.3"
  }
}
