{
  "name": "@holocene/bloodbank-client",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint:todo'",
    "test": "echo 'test:todo'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@holocene/module-sdk": "workspace:*",
    "@nats-io/jetstream": "^3.4.0",
    "@nats-io/transport-node": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
